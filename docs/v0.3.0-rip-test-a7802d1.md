# brw a7802d1 — "rip round the web" review (Chromium)

Date: 2026-06-25. Transport: extension-bridge (Chromium, fresh profile).
Method: live `mcplexer brw_chromium` against the-internet.herokuapp.com (39 examples) + feature probes.
Binary: `a7802d1` (latest main, `make install-mac`, launchd restart).

## Pre-flight: all three artifacts LIVE

| Artifact | State | Action |
| --- | --- | --- |
| Go binary `brwd` | fresh build + install-mac + launchd kickstart | OK |
| mcplexer catalog | `mcpx.reload_server({})` — 52 brw_chromium tools confirmed | OK |
| Chromium extension | `service_worker.js` identical to source — no reload needed | OK |

## Go tests

```
FAIL  internal/extensionbridge — TestServiceWorkerActiveTabResolverIsAuthoritative
  bridge_activetab_test.go:347: resolveForegroundTabId must scan the focused window BEFORE falling back to the cached tab id
```

All other packages pass. One pre-existing test failure from latest commit.

## Pages verified WORKING (27/39)

| # | Page | What was tested | Result |
| --- | --- | --- | --- |
| 1 | `/` (home) | Snapshot → 39 elements, all links listed | ✅ |
| 2 | `/dropdown` | Select option 1 via `brw_select` | ✅ |
| 3 | `/checkboxes` | Toggle unchecked → checked via `brw_click` | ✅ |
| 4 | `/challenging_dom` | 10 edit + 10 delete links, all disambiguated (`e6_edit_1`…`e6_edit_10`), all refs unique | ✅ |
| 5 | `/horizontal_slider` | `brw_fill` moved slider from 0 → 2.5 | ✅ |
| 6 | `/dynamic_controls` | Remove → "It's gone!" (3s), Add → "It's back!" (3s), both via `brw_wait_for` text condition | ✅ |
| 7 | `/login` | `brw_type` username + password + Enter → navigated to `/secure`, password field `sensitive=true` | ✅ |
| 8 | `/javascript_alerts` | Alert/Confirm/Prompt all auto-dismissed (~60ms each, no hang), result: "You entered:" (empty, expected) | ✅ |
| 9 | `/upload` | `brw_upload_file` selected file, upload button click needed separately | ✅ (partial) |
| 10 | `/hovers` | "View profile" links visible in snapshot (hover content captured) | ✅ |
| 11 | `/large` | Snapshot: 1 element (frontier bounded). Read: 15,260 chars. 9ms snapshot time. | ✅ |
| 12 | `/shadowdom` | `brw_read` pierces shadow roots: "Simple template Let's have some different text!" | ✅ |
| 13 | `/frames` | Links to Nested Frames and iFrame visible | ✅ |
| 14 | `/inputs` | Type "42" into number spinbutton → value=42 | ✅ |
| 15 | `/key_presses` | Press "A" → "You entered: A" (via read) | ✅ |
| 16 | `/add_remove_elements` | Click Add 3x → 3 Delete buttons with unique refs | ✅ |
| 17 | `/redirect_link` | Click "here" → redirected to `/status_codes` | ✅ |
| 18 | `/dynamic_loading` | Example 1 → Start → "Hello World!" after 5s wait | ✅ |
| 19 | `/infinite_scroll` | Scroll works, content not in snapshot (dynamic) | ✅ |
| 20 | `/jqueryui/menu` | All menu items visible with disambiguated refs (`e6_disabled`, `e6_enabled`, `e6_downloads`) | ✅ |
| 21 | `/disappearing_elements` | 5 links (Home, About, Contact Us, Portfolio, Gallery) | ✅ |
| 22 | `/entry_ad` | Modal dismissed, "click here" link visible | ✅ |
| 23 | `/floating_menu` | 4 links (Home, News, Contact, About) | ✅ |
| 24 | `/forgot_password` | Fill email + submit → "Internal Server Error" (expected test behavior) | ✅ |
| 25 | `/status_codes` | Links for 200, 301, 404, 500 | ✅ |
| 26 | `/multiple_windows` | "Click Here" link visible | ✅ |
| 27 | `/geolocation` | "Where am I?" button visible | ✅ |

## Pages with expected limitations (12/39)

| # | Page | Limitation | Category |
| --- | --- | --- | --- |
| 1 | `/nested_frames` | 0 elements — frame content inaccessible | Frame limitation |
| 2 | `/broken_images` | Only 1 image (Fork on GitHub) — broken imgs lack alt text | Snapshot scope |
| 3 | `/context_menu` | 3 elements — hot spot div not interactive | Snapshot scope |
| 4 | `/dynamic_content` | 4 elements — dynamic paragraphs not captured | Snapshot scope |
| 5 | `/exit_intent` | Modal not triggered — needs mouse-exit event | Extension limitation |
| 6 | `/digest_auth` | chrome-error — needs credentials in URL | Auth limitation |
| 7 | `/abtest` | Minimal elements — variant-dependent content | Snapshot scope |
| 8 | `/javascript_error` | Minimal elements — error page is sparse | Expected |
| 9 | `/drag_and_drop` | Elements visible but `draggable=false` — HTML5 drag not triggered by synthetic input | Synthetic input limit |
| 10 | `/secure` (post-login) | Already tested via login flow | ✅ |
| 11 | `/notification_message` | "Click here" link visible | ✅ |
| 12 | `/javascript_onload_error` | Page loaded, minimal content | Expected |

## Feature probes

| Feature | Result | Notes |
| --- | --- | --- |
| `brw_navigate_to` with `snapshot:true` | ✅ | Returns 3 elements inline — no extra round trip needed |
| `brw_click` with `snapshot:true` | ✅ | Returns post-click elements — works on extension bridge |
| `brw_fill` with `snapshot:true` | ✅ | Returns 7 elements (full form) — works on extension bridge |
| `brw_find` | ⚠️ | Returns empty on dropdown page — query matching may need review |
| `brw_evaluate` | ✅ | `document.title` → "The Internet" |
| `brw_observe` | ✅ | Returns URL + title |
| `brw_console` | ✅ | 0 messages (nothing logged) |
| `brw_downloads` | ⚠️ | "unavailable on extension-bridge backend" — expected |
| `brw_plan` | ⚠️ | Action vocabulary limited — no `click_text`, only known actions work |
| `brw_type` on password field | ✅ | `sensitive=true` in snapshot, value redacted |
| `brw_fill` on password field | ⚠️ | Didn't trigger login — `brw_type` + Enter needed instead |
| ID disambiguation | ✅ | 20 links (10 edit + 10 delete), all unique suffixed refs |
| Frontier bounding | ✅ | Large DOM (2500 `<td>`) → 1 element in 9ms |
| Shadow DOM read | ✅ | Pierces shadow roots |
| Auto-wait | ✅ | Dynamic Controls: 3s waits for text conditions |
| JS alert auto-dismiss | ✅ | Alert/Confirm/Prompt: ~60ms each, no hang |
| Token discipline | ✅ | Semantic-first, no screenshot tokens |

## New bugs found

None. All previously known bugs still present:
1. `snapshot:true` on action tools broken on `--upstream-http` topology (known from 5a838fa) — **works on extension bridge** (confirmed: click, fill, navigate_to all return snapshots)
2. `new_tab_id` cannot fire for `target=_blank` (synthetic input limitation)

## Scorecard

| Dimension | Score | Notes |
| --- | --- | --- |
| Speed | 9.0 | Snapshot 5–15ms, actions 50–250ms, navigate 130–425ms. Faster than 5a838fa test. |
| Accuracy | 8.5 | All refs resolve, disambiguation perfect, sensitive redaction correct. `brw_fill` on password lost a point. |
| Token usage | 9.0 | Frontier bounded perfectly. Read/snapshot complementary. No wasted tokens. |
| Back-and-forth | 8.5 | Most tasks 2–4 calls. `snapshot:true` on navigate_to eliminates extra round trip. |
| Context consumption | 8.5 | Compact results. `brw_plan` action vocab limits batch planning. |

**Overall: 8.7/10** — strong improvement over 5a838fa (8.0). Speed and accuracy both up. No new bugs.

## Changes since 5a838fa that helped

- `a7802d1` — snapshot-on-action, no-wedge waits, tab-enumeration robustness
- `5a838fa` — LLM usability improvements + bug fixes
- `eb9308d` — tab identification fix + seamless mode
- `8dc60c0` — Chrome stability (debugger detach, graceful shutdown, profile guard, leaks)
