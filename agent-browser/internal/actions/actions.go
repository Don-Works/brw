package actions

// Package actions is reserved for higher-level, auditable action composition.
// The MVP keeps primitive action execution on browser.Manager so HTTP and MCP
// share exactly the same behavior.
